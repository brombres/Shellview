uses UI/ConsoleUI
uses Utility/TextCanvas

class ShellViewUI : ConsoleUI [singleton]
  PROPERTIES
    image_view : ImageView
    filepaths  : String[]

  METHODS
    method init
      prior.init
      image_view = ImageView()
      add( image_view )

endClass
