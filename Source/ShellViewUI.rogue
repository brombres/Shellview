uses UI/ConsoleUI
uses Utility/TextCanvas

class ShellViewUI : ConsoleUI [singleton]
  PROPERTIES
    bitmap_view : BitmapView

  METHODS
    method init
      prior.init
      bitmap_view = BitmapView()
      add( bitmap_view )

endClass
