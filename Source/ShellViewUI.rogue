uses UI/ConsoleUI
uses Utility/TextCanvas

class ShellViewUI : ConsoleUI [singleton]
  PROPERTIES
    bitmap_view : ImageView

  METHODS
    method init
      prior.init
      bitmap_view = ImageView()
      add( bitmap_view )

endClass
